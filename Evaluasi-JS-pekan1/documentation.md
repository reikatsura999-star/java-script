1.variable
let,var,const adalah deklarasi yg memuat sebuah value

2.di sebuah function terdapat sebuah paramater untuk menyimpan data.

3.setelah data di masukan di paramater lalu di eksekusi di dalam function.

4.di function  data akan di melewati if else atau switch,logika aritmatika,logika,looping : for,while,forwhile semuanya akan di seleksi value dari si vaiable apakah true atau false.

5.setelah di seleksi di dalam operator/function, maka selanjutnya masuk di console dan di tampilkan di web.